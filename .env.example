# Discord bot token (required)
DISCORD_TOKEN=

# Audio device name substring to match
AUDIO_DEVICE=ID:Core

# Audio backend: sounddevice or ffmpeg (default: sounddevice)
AUDIO_BACKEND=sounddevice

# Playback volume multiplier (default: 1.0)
VOLUME=1.0

# Log selected backend/device/volume without exposing secrets
DEBUG_CONFIG=false

OWNER_ID=your_numeric_discord_user_id

GUILD_ID=your_server_id

LOG_FILE=blackstar-bot.log
